services:
  - type: web
    name: njit-empty-room-finder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    healthCheckPath: /health
    autoDeploy: true
